services:
  drive-manager:
    image: capitanpio/drive-manager:latest
    container_name: drive_manager
    ports:
      - "8081:8081"
    env_file:
      - .env          # create this file on the VPS with all required secrets
    restart: unless-stopped